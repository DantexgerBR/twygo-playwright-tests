# Núcleo da suíte de testes/validação Playwright
pytest>=8.0.0
pytest-playwright>=0.5.0
playwright>=1.45.0
python-dotenv>=1.0.0

# Upload de evidências (scripts/upload_evidencias.py)
cloudinary>=1.40.0

# NOTA: dependências do app Flet (cancelado) e do agente LLM (descontinuado) foram
# removidas — flet, pillow, pyperclip, httpx, pypdf, keyring, gradio, anthropic,
# jinja2, google-genai, groq. O código legado vive em app/ (congelado) e seus
# testes em tests/app/ (ignorados no pytest.ini). Se precisar rodar o app legado,
# instale essas libs separadamente.
